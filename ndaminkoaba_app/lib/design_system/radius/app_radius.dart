import 'package:flutter/material.dart';

class AppRadius {
  AppRadius._();

  static final small = BorderRadius.circular(12);

  static final medium = BorderRadius.circular(18);

  static final large = BorderRadius.circular(24);

  static final extraLarge = BorderRadius.circular(32);

  static final circle = BorderRadius.circular(100);
}